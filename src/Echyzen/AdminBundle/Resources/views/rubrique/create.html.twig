{# src/Echyzen/Admin/Resources/views/rubrique/create.html.twig #}
{% for flashMessage in app.session.flashbag.get('notice') %}
    <div class="flash-notice">
        {{ flashMessage }}
    </div>
{% endfor %}


<h2>Rubrique creation</h2>
<form id="lol" method="post" class="ajax_form" {{ form_enctype(form) }} action="{{ path('admin_rubrique_create') }}">
    {{ form_widget(form) }}
    <input type="submit" />
</form>

